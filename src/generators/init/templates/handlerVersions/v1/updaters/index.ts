/**
 * Export updaters here.
 *
 * This file is automatically updated when using `demux generate updater v1 [ actionType ]`. The only changes you may want
 * to make are the order of the items between `ARRAY ITEMS START` and `ARRAY ITEMS END`, as this will be the order in
 * which they execute.
 */

// IMPORT START
import exampleUpdater from './updater'
// IMPORT END

export const updaters = [
  // ARRAY ITEMS START
  exampleUpdater,
  // ARRAY ITEMS END
]
