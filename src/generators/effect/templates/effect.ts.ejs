/**
 * Use Effects for executing non-deterministic mutative actions. They are run asynchronously, and may be run concurrently
 * with other effects. Make sure to export one (and only one) Effect object from this file.
 */

import { Effect, BlockInfo, StatelessActionCallback } from 'demux'

const run: StatelessActionCallback = async (payload: any, blockInfo: BlockInfo, context: any) => {
  // Put your effect code here
}

const effect: Effect = {
  run,
  actionType: '<%= contractName %>::<%= actionName %>', // The actionType this effect will subscribe to
  deferUntilIrreversible: <%= deferUntilIrreversible %>, // If true, the effect will only run after the action becomes irreversible
}

export default effect
